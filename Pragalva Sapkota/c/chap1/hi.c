#include<stdio.h>
int main ()
{
    printf ("input the number:");
    float num;
    scanf("%f",&num);
    printf("the number is:");
    return 0;
}
/* The control string contains only one conversion specification character %F.which implies that one floating point value should be entered as input,and this entered value will be stored in the variable height */