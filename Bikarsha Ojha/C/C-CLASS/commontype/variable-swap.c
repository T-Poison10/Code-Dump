#include <stdio.h>

int main() {
    int a, b,swap;
    a = 10;
    b = 20;

    swap = a+b;

    printf("a variable was 10, Swapped variable of a: %d\n",swap-a);
    printf("b variable was 20, ,7Swapped variable of b: %d", swap -b);

    return 0;
}
