#include<stdio.h>
int main(){
    int n;
    printf("Enter a number");
    scanf("%d",n);
    if(n > 10){
        printf("NUMBER > 10");
    } else {
    printf("moving ahead with another set of instructions");
    }
    return 0;
}